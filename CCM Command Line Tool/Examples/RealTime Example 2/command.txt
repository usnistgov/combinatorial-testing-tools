java -jar ccmcl.jar -A ACTSfile.txt -S -B -H -p -T 2,3,4,5,6 -R -e generate_fastest.jar 


NOTE: The executed jar file is generating tests VERY fast, so it is best to leave the 
      update interval and max thread count at the default values by using the command
      above. However, if you want to experiment with the update-interval and thread 
      max, view the help menu.